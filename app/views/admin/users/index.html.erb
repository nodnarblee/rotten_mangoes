<h1> INDEX </h1>

 <%= paginate @users %>
<div> 
<% @users.each do |user| %> 
  <h2><%= user.firstname %> : </h2>
 <h3><%= user.email %></h3> 
 <%= link_to "delete", admin_user_path(user.id), method: :delete, confirm: "do it..." %>
 <%= link_to "edit", edit_admin_user_path(user)%>
 <br>
 
<% end %>
</div>

